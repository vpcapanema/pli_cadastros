// moved from inline <script>

        // Inicializa scripts específicos da página solicitacoes-cadastro
        DataTableScriptLoader.initDataTableScripts([
            '/static/js/pages/solicitacoes-cadastro.js'
        ]).then(() => {
            console.log('Página solicitacoes-cadastro inicializada com sucesso');
        }).catch(error => {
            console.error('Erro na inicialização da página solicitacoes-cadastro:', error);
        });
    
;

        // Inicializa scripts específicos da página solicitacoes-cadastro
        DataTableScriptLoader.initDataTableScripts([
            '/static/js/pages/solicitacoes-cadastro.js'
        ]).then(() => {
            console.log('Página solicitacoes-cadastro inicializada com sucesso');
        }).catch(error => {
            console.error('Erro na inicialização da página solicitacoes-cadastro:', error);
        });
    
